name: Build and Push

on:
  push:
    branches: [main]
    paths:
      - "Dockerfile"
      - "*.sh"
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
  security-events: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/docker-release.yaml@bbb84133e2512f24cf9e89fc67d43d378d299360 # v1
    with:
      registries: "ghcr"
    secrets: inherit
