name: Build and Push

on:
  push:
    branches: [main]
    paths:
      - "Dockerfile"
      - "*.sh"
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
  security-events: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/docker-release.yaml@e69f2cbaa1ee46d48cbabf7d0858ae46720152ef # v1
    with:
      registries: "ghcr"
    secrets: inherit
